#include <stdio.h>
#include "main.h"

/**
 * main - Print the name of the program
 * @argc: Cout arguments
 * @argv: Arguments
 *
 * Return: Always 0 (Succes)
 */

int main(int argc, char *argv[])
{
(void) argc;
printf("%s\n", argv[0]);

return (0);
}
