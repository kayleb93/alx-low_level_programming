#include <stdio.h>
#include "main.h"

/**
 *_puts - prints a string
 * @str: string to print
 *
 * Description: prints a string
 * On success: returns no error
 */

int _strlen(char *s)
{
	int i = 0;

	while (*(strlen + i) != '\0')
	{
		putchar(*(strlen + i));
		i++;
	}
	putchar(10);
}
