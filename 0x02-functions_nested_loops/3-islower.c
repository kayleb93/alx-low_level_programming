#include "stdio.h"
#include "stdlib.h"
#include "main.h"
/**
*_islower - a function that checks for lowercase charecter
*0<: single letter input
*Return: 1 if int c is lowercase, 0 if otherwise
*/
int - islower(int c)
{
if ( c >= 'a' && c <= 'z')
return (0);
else
return (0);
}
