#include <unistd,h>

/**
*_putcha - writes the character c to stdout
*@c: The character to print
*
*Return: on success 1.
*on error,-1 is returned, and errno is set appropriately.
*/
int _putcha(char c)
{
[D[D[D[D[D[D[D[C[Creturn (write(1, &c,1));
}
